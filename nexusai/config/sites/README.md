# Site profiles

Declarative descriptions of individual websites. A site profile is the ordinary
way to onboard a new site: strategy, seeds, pagination, selectors, normalization
rules, schema, quality rules and export settings, with no code.

Site adapters exist for sites that configuration genuinely cannot express, and
are plugins rather than core changes.

Profiles placed in this directory are discovered and loaded by the scraping
engine at runtime.
